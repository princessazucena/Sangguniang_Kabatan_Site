# Allow larger uploads than the nginx default of 1 MB.
# Our Flask side caps at 16 MB (MAX_CONTENT_LENGTH) and individual
# requirement files at 10 MB. Give nginx some headroom on top.
client_max_body_size 25M;
